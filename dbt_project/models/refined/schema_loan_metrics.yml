version: 2

models:
  - name: loan_metrics
    description: "Loan-level metrics demonstrating cohorting and accrual macros"
    tests:
      - not_null:
          column_name: loan_id
      - not_null:
          column_name: total_repaid
